# Go
apps/api/bin/
apps/api/.gocache/
apps/api/.env
*.test

# Node / SvelteKit
node_modules/
apps/client/.svelte-kit/
apps/client/build/
apps/client/.env

# Local dev database
.local/

# Uploaded media & generated stream segments (runtime data).
# NB: HLS segments are *.ts — scope to the runtime dirs so we don't ignore
# TypeScript source files.
data/
storage/
/apps/api/data/

# OS / editor
.DS_Store
*.log
